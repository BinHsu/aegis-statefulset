# =============================================================
# Private context — NEVER committed to public repo
# =============================================================
# These files contain personal AI-tool operating rules and
# assignment-specific context. They are local tooling for development;
# the public submission must not include them.
CLAUDE.md
AGENTS.md
.claude-rules/
.claude/
_context/

# =============================================================
# Terraform
# =============================================================
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.auto.tfvars
# Saved plan output from `terraform plan -out=tfplan` — operator-local,
# regenerated on every apply; do not commit (binary, may contain secrets).
tfplan
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# =============================================================
# Helm
# =============================================================
charts/*.tgz
*.tgz

# =============================================================
# Secrets / Credentials
# =============================================================
.env
.env.*
!.env.example
*.pem
*.key
*.crt
credentials.json
**/secrets/

# =============================================================
# macOS
# =============================================================
.DS_Store
.AppleDouble
.LSOverride

# =============================================================
# IDE / Editor
# =============================================================
.vscode/
.idea/
*.swp
*.swo
*~
.netrwhist

# =============================================================
# Build / Logs
# =============================================================
node_modules/
dist/
/build/         # repo-root build/ only; NOT scripts/build/ which is real source
*.log
logs/

# =============================================================
# Python (if any tooling scripts)
# =============================================================
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/

# =============================================================
# Backup files
# =============================================================
*.bak
*.backup
*.orig

# =============================================================
# Chaos demo evidence — intermediate output captured Thu morning;
# DR_report.md intermediate + raw evidence captures are not committed
# (final artifact ships as docs/DR_report.pdf which is committed).
# =============================================================
chaos-evidence/

# =============================================================
# Runbook config — working copy generated from .example by
# scripts/dev/runbook-00-bootstrap.sh. The .example is canonical
# committed; the working file allows forker / operator local
# customisation without polluting the repo.
# =============================================================
scripts/dev/runbook-config.yaml

# =============================================================
# Submission PDFs — generated fresh via scripts/build/render-submission-pdfs.sh
# from their canonical .md sources (1:1 .md ↔ .pdf mapping). Generated
# at Thu morning per runbook 05; attached to submission email. Source-of-
# truth is .md; PDFs are derived artifacts, not committed.
# =============================================================
docs/submission-pdfs/

# =============================================================
# Terraform state backend config (generated from
# bootstrap module output; never committed)
# =============================================================
infrastructure/terraform/backend.hcl
infrastructure/terraform/bootstrap/terraform.tfstate
infrastructure/terraform/bootstrap/terraform.tfstate.backup
infrastructure/terraform/bootstrap/.terraform.lock.hcl
